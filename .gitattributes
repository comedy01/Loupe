# Normalize text files to LF in the repository; let checkouts use the platform default.
* text=auto

# Linux start script should use lf
/gradlew        text eol=lf

# These are Windows script files and should use crlf
*.bat           text eol=crlf

# Binary
*.jar           binary
*.png           binary
